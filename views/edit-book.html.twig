<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link href="css/styles.css" rel="stylesheet" type="text/css">
    <title>Library</title>
</head>
<body>
    <div class="container">
        <h1>{{ book.getTitle }}</h1>
        <form action="/edited_book/{{ book.getId }}" method="post">
            <input name="_method" type="hidden" value="patch">
            <div class="form-group">
                <label for="new_title">Edit title:</label>
                <input name="new_title" class="form-control" placeholder="{{book.getTitle}}">

                <label for="author_id">Add an existing author:</label>
                <select name="author_id" class="form-control">
                    <option value="0"></option>
                    {% for author in authors %}
                    <option value="{{author.getId}}">{{author.getName}}</option>
                    {% endfor %}
                </select>

                <label for="new_author">Add another author:</label>
                <input name="new_author" class="form-control" value="">
            </div>
            <button type="submit" class="btn btn-success">Submit</button>
        </form>
    </div>
</body>
</html>
